import React from 'react';

import Logo from '../../assets/image.png';

const logo = (props) => (
    <div className="logo">
        <img src={Logo} alt="Groupomania" height='300' />
    </div>
)

export default logo;